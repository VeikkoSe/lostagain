class TextTimer {
    constructor() {
        this.events = {};
        this.events.push('5000','What happened?');
        this.events.push('2000','We were crushed!');
        this.events.push('2000','That seems to be the case');
        this.events.push('2000','We need to heal our wounds and rebuild your ship.');
        this.events.push('2000','True. And we still have the tracker on their flag ship. We should see it on your overview map.');
        this.events.push('2000','When the time is right we will strike back!');
        this.events.push('2000','My sensor will show the nearest asteroid that we need to mine so we can get minerals for fuel');
        this.events.push('2000','Oh yes. These old ships still can\'t jump without fuel');
        this.events.push('2000','Asteroid should not be far');
    }

}